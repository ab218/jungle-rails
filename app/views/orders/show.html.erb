<section class="orders-show">

  <header class="page-header">
    <h1>Order #<%= @order.id %></h1>
  </header>
<body>
  <div>
  <h4>Thank you for your order, <%= @current_user.name %> !</h4>
  <%= link_to "Continue Shopping", root_path, class: 'btn btn-lg btn-primary thanks' %>
  </div>

    <table class="table table-bordered">
      <thead>
        <tr>
          <th>Product</th>
          <th>Image</th>
          <th>Price</th>
          <th>Quantity</th>
          <th>Total</th>
        </tr>
      </thead>
      <tbody>
        <% @line_items.each do |line_item| %>
          <%= render 'item', line_item: line_item %>
        <% end %>
      </tbody>
      </body>

</section>
<footer class="orderTotal">
<h4>Grand Total: <%= humanized_money_with_symbol @order.total_cents %></h4>
      </footer>