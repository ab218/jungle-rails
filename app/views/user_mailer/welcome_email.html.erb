
<!DOCTYPE html>
<html>
  <head>
    <meta content='text/html; charset=UTF-8' http-equiv='Content-Type' />
  </head>
  <body>
    <h1>Thank you for the purchase</h1>
    <p>
      You have successfully purchased something.
    </p>
    <% @order.line_items.each do |line_item| %>
        <% if product = Product.find_by(id: line_item.product_id) %>

<tr>
    <td>
      <h4><%= product.name %></h4>
      <br>
      <p><%= product.description %></p>

      <td>
      <%= image_tag product.image.url, class: 'tinyImg' %>
    </td>
    </td>
    <td>
    <%= product.price %>
  </td>
  <td>
  <%= line_item.quantity %>
</td>
<td>
<%= line_item.quantity * product.price %>
</td>
  </tr>
      <% end %>
      <% end %>
      <footer class="orderTotal">
<h4>Grand Total: <%= humanized_money_with_symbol @order.total_cents %></h4>
      </footer>
  </body>
</html>