
<% if review = Product.find_by(id: review.product_id) %>

<tr>
    <td>
      <h4><%= product.name %></h4>
      <br>
      <p><%= product.description %></p>
      <td>
      <%= image_tag product.image.url, class: 'tinyImg' %>
    </td>
    </td>
    <td>
    <%= product.price %>
  </td>
  <td>
  <%= line_item.quantity %>
</td>
<td>
<%= line_item.quantity * product.price %>
</td>

</tr>
  <% end %>